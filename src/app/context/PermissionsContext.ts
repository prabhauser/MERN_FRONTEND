import React from 'react';

// type Permissions = {
//     CAN_VIEW_SUPERVISOR_LINK: boolean;
//     permission: string[];
// };

//const PermissionsContext = React.createContext<Permissions | null>(null);
const PermissionsContext = React.createContext<any | null>(null);

export default PermissionsContext;
